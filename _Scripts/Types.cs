public enum ValueType
{
    BaseValue, Modifier
}

public enum StatType
{
    MinimumDamage, MaximumDamage, CriticalHitMultiplier, CriticalHitChance, FireRate, ReloadTime, Precision, Range, MagazineSize
}

public enum ItemType
{
    Weapon, Mod, GeneratedItem
}

public enum WeaponType
{
    Handgun, Shotgun, AssaultRifle
}

public enum ModType
{
    Action, Barrel, Muzzle, Magazine, Stock, Sight
}

